import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Toaster } from "@/components/ui/toaster"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Dickwella Construction - Equipment Rental Management System",
  description:
    "Professional equipment rental management system for construction companies. The pioneers of building material suppliers and government contractors.",
  keywords: "construction, equipment rental, machinery, tools, Sri Lanka, Dickwella",
  authors: [{ name: "Dickwella Construction" }],
  viewport: "width=device-width, initial-scale=1",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
        <Toaster />
      </body>
    </html>
  )
}
